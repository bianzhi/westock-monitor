# Westock Monitor · 项目约定（AI 自动读取，用于跨会话接续上下文）

## 核心原则

1. **数据流**：采集先落库，页面/API 一律只读库，不在请求路径上同步调外部接口。新数据入库后，前端通过轮询 `/api/health` 的 `cache_updated` 自动刷新，不做手动刷新。
2. **数据源优先级**：所有数据优先查 westock CLI，只有 westock 实在查不到、又确实需要，才走腾讯/东财兜底，不可本末倒置。
3. **Python 依赖**：装在项目 `.venv`，测试与模块导入必须用 `.venv/bin/python3 -m pytest`（系统 python3 缺 pydantic/pytest 等包）。

## 关键约定 / 陷阱

- **westock CLI 实测命令与老文档不一致**（1.0.5）：`fund flow` / `search --type sector` / `sector ranking --raw` 才是正确命令；`asfund/minute/quote/board/sector list sw2` 已废弃。任何"参考文档"都要实测一遍命令存在性与返回字段。
- **申万行业分类清单**必须从权威来源（用户清单/申万官网）出发，不可凭记忆编造。
- **涨停家数口径**：沪深（不含北交所 30% 涨停），采集时剔除涨跌幅 > 21%。
- **limit_up_pool 的衍生率不落库**：封单率（fund/ltsz）、净额率（main_net_inflow/amount）在 API 端点现算（`/api/limit-up` 与 `/api/review34` 都是）。
- **review34 连板复盘判定对齐 Excel《3j4_复盘》套表**：情绪锚点以「3进4晋级率」为准（炸板率/最高连板仅作辅助备注）；板块主线阈值 ≥5（可自动算维度满分 7，对齐 Excel 的 7/10 比例）；个股入选 = 炸板≥3 一票否决 + 封板质量（硬板/回封板/烂板）。

## 演进脉络（一路的过程）

1. **日线图**：修复涨跌幅 554.67%（前端 K 线反推累积放大脏数据 → 改为直读落库 `change_pct`）；补齐最近 50 交易日涨跌幅+资金（westock `fund flow --date` + `ClosePrice` 差分）；K 线改用真实收盘价 `close_price`（`sector_daily` 落库）。
2. **涨停池**：综合信息（封板率/连板梯队/晋级率，晋级率为时序口径「今日 N 板 ÷ 昨日 N-1 板」）；筛选（连板/行业/时段）+ 所属概念 + 行业/概念超链接；数据校正（涨停家数剔除北交所、先删后插落库）。
3. **连板复盘**：理念源自《3j4_复盘》Excel 套表（五层漏斗：大盘 S1→板块 S2→个股 S3→执行 S4→次日 S5 + 量化评分），扩展为「连板」页（1进2/2进3/3进4/4进5 阶段切换），S1/S2 通用、S3 按 stage 筛 N 板个股。个股分时图页面待做（代码/名称列已留超链接位）。
